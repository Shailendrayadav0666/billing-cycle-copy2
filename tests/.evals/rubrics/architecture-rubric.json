{
  "rubricName": "Architectural Alignment - Billing-Cycle Mid-Cycle Subscription Upgrade",
  "rubricVersion": "1.0.0",
  "derivedFrom": ["spec/plans/architecture.md#10-verifiable-constraints"],
  "evalCriteria": [
    {
      "id": "ARCH-01",
      "metric": "Server-side-only proration",
      "weight": 0.20,
      "prompt": "No changed frontend file (Billing.jsx) contains proration arithmetic (subtraction/division/multiplication of plan prices or date deltas). Score 0 if any such computation appears client-side.",
      "source": "spec/plans/architecture.md#10 ARCH-01"
    },
    {
      "id": "ARCH-02",
      "metric": "Deterministic dummy gateway",
      "weight": 0.20,
      "prompt": "The charge_card function body contains no random module use, no requests/httpx/network call, no datetime.now()/time.time() call. Score 0 if any of these appear inside charge_card.",
      "source": "spec/plans/architecture.md#10 ARCH-02"
    },
    {
      "id": "ARCH-03",
      "metric": "No partial mutation on failure",
      "weight": 0.25,
      "prompt": "The declined-path code branch (checked before any mutation) returns/raises before any assignment to users[email] or billing_data[email]. Score 0 if any mutating statement executes before the charge_card result is checked.",
      "source": "spec/plans/architecture.md#10 ARCH-03"
    },
    {
      "id": "ARCH-04",
      "metric": "Already-Premium guard on both endpoints",
      "weight": 0.20,
      "prompt": "Each of GET /api/billing/upgrade-preview and POST /api/billing/upgrade contains an early guard clause checking plan_name == 'Premium' ahead of proration/gateway logic. Score 0 if either endpoint is missing this guard or the guard runs after a mutation.",
      "source": "spec/plans/architecture.md#10 ARCH-04"
    },
    {
      "id": "ARCH-05",
      "metric": "No new dependencies",
      "weight": 0.15,
      "prompt": "Diff of backend/requirements.txt and frontend/package.json shows no added dependency line. Score 0 if either file gains a new package for this feature.",
      "source": "spec/plans/architecture.md#10 ARCH-05"
    }
  ]
}
